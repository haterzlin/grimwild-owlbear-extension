/**
 * Canonical source-facing data loader for external Grimwild content.
 * The current implementation delegates to transitional helpers in `src-like/`.
 */

export { loadExternalData } from "../../src-like/data-loader.js";
